apiVersion: v1
kind: Service
metadata:
    name: yesodweb
    labels:
        app: yesodweb
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 3001
      protocol: TCP
    selector:
        app: yesodweb
